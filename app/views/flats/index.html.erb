<div class="container">
  <h1>Our flats</h1>
  <%@flats.each do |flat| %>
    <ul>
      <li><%=link_to flat.name, flat_path(flat) %></li>
    </ul>
  <% end %>
  <%= link_to "Add a new flat", new_flat_path %>
</div>
